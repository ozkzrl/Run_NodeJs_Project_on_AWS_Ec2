<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bookstore</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <h1>📚 Bookstore</h1>
    <a href="/add-book">Add New Book</a>
    <ul>
        <% books.forEach(book => { %>
            <li>
                <a href="/book/<%= book.id %>"><%= book.title %> - <%= book.author %> ($<%= book.price %>)</a>
            </li>
        <% }) %>
    </ul>
</body>
</html>
